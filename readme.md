# Bruteforce various tools on images

1. install various tools `chmod +x tools.sh && ./tools.sh ` 
2. put the target files on `images` folder
3. `chmod +x ./run.sh`
4. run the script `./run.sh` 
5. examine the files on res 